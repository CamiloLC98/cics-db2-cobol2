DSN    SYSTEM       (DB9G)
BIND   PACKAGE      (APPCOLL)  -
       MEMBER       (BLOQCOB)  -
       ACTION       (REPLACE)  -
       ISOLATION    (CS)       -
       VALIDATE     (BIND)     -
       ENCODING     (EBCDIC)   -
       FLAG         (I)        -
       RELEASE      (COMMIT)
END
