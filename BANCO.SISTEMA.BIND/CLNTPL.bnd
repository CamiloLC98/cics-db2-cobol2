DSN    SYSTEM       (DB9G)
BIND   PLAN         (CLNTPL)     -
       PKLIST       (APPCOLL.*)  -
       ACTION       (REPLACE)    -
       ISOLATION    (CS)         -
       VALIDATE     (BIND)       -
       ENCODING     (EBCDIC)     -
       FLAG         (I)          -
       RELEASE      (COMMIT)
END
